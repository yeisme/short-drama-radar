# 实施与验收任务

由 scripts/openspec-tasks.py 维护状态。

- [x] 1.1 owner=Radar；scope=运行锁；lane=domain；实现 src/runlock.ts（radar.lock 独占创建、活进程等待、死锁接管、只释放自己的锁、RADAR_LOCK_WAIT_MS 覆盖）；验收=排队/超限/接管/只读不持锁语义全部成立；验证=bun test test/unit/runlock.test.ts --timeout 30000；失败修锁协议。 | evidence: bun test test/unit/runlock.test.ts --timeout 30000 -> 6 pass/0 fail（创建/释放、活进程等待超限 lock_busy 带持有者、等待后接续、死 PID 接管、释放不夺被接管锁、probe 只读）
- [x] 1.2 owner=Radar；scope=分发层加锁；lane=integration；在 CLI 分发层为 run/collect/score/card/cluster build 与 market observe/analyze/brief/sync 获取运行锁，run 内部动作不重复加锁；验收=重叠进程排队且只读命令不受影响；验证=bun test test/unit/runlock.test.ts --timeout 30000；失败检查分发点覆盖。 | evidence: test/integration/cli-contract.test.ts 新增 lock_busy 场景（预置活 PID 锁 + RADAR_LOCK_WAIT_MS=0 跑 radar score -> 非零退出且输出含 lock_busy+pid）；run 内部动作单次加锁无重入
- [x] 1.3 owner=Radar；scope=install 退役；lane=contract；schedule install（全部 backend）与 market schedule install 改为 command_retired 零写入，删除三平台单元生成器与 market 单元生成器；schedule show/market schedule show 去掉 install action；验收=退役错误码+文档指引，grep 无单元生成残留；验证=bun test --timeout 30000；失败清遗漏引用。 | evidence: cli-contract 新增 2 个 command_retired 断言 + schedule show 无 install action；grep src/ scripts/ 对 buildScheduleUnits/buildLaunchdUnits/buildWindowsUnits/buildMarketScheduleUnits/systemdUserDir 等零命中；schedule-plan 死亡生成助手一并清除
- [x] 1.4 owner=Radar；scope=doctor 重定向；lane=contract；doctor schedule 检查改为锁可用性+计划可读（零等待试取即释放），移除 OS 管理器探测与启用命令输出；验收=无管理器容器不再因单元未安装降级，锁被持有时 degraded 带 pid；验证=bun test test/unit/schedule-health.test.ts --timeout 30000；失败修探测语义。 | evidence: test/unit/schedule-health.test.ts 新 doctor 断言（free=ok 含 customer-owned、held=degraded 带 pid）；实测 doctor --json schedule={ok, run lock available...}（本容器无 systemd 也不再降级）
- [x] 1.5 owner=Radar；scope=测试收口；lane=verification；删除 schedule-backends/market-schedule golden 测试，适配 secrets/schedule-health/相关集成测试，新增 runlock 与退役行为断言；验收=全量 bun test 与 bun run typecheck 通过；验证=bun test --timeout 30000 && bun run typecheck；失败逐个归因修复。 | evidence: bun test --timeout 30000 -> 430 pass/0 fail/2 skip（docker PG 既有豁免）；bun run typecheck 通过
- [x] 1.6 owner=Radar；scope=文档与治理；lane=handoff；重写 docs/runtime/schedule.md 为客户侧接线指南（cron/launchd/schtasks 示例+锁语义），更新 global-market-radar/canary-runbook/AGENTS.md 表述；验收=文档无"radar 生成单元"残留表述；验证=openspec validate radar-scheduler-retirement-v1 --strict --no-interactive；失败修 readiness 声明。 | evidence: docs/runtime/schedule.md 重写为客户侧接线指南（建议时刻表+锁语义+Linux cron/systemd+macOS launchd/cron+Windows schtasks）；global-market-radar 3 处、canary-runbook 2 处、AGENTS.md 所有权行、market observe 提示更新；openspec validate radar-scheduler-retirement-v1 --strict --no-interactive -> Change is valid
