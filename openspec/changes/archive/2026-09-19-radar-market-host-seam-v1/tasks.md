## 1. host seam 交付

单 writer 串行交付；DSH 消费者代码只读参考，不在本仓修改。

- [x] 1.1 实现协议合同与帧 schema；owner=radar；scope=src/market/host-serve.ts 帧循环、src/market/cli.ts host-serve 路由、src/cli.ts help；依赖=无；验收=stdin/stdout NDJSON 帧、stderr 仅诊断、串行处理、畸形行 frame_invalid 不中断、shutdown 退出；验证=bun test test/unit/market-host-serve.test.ts。证据：2026-09-19 marketHostServe 以 readline 逐行串行处理 radar.market.host.frame.v1 帧，每请求恰好一行响应；非 JSON/schema 不符/未知 op 返回 `id:null` 的 frame_invalid 且循环继续（JSON parse 失败单独映射 frame_invalid）；shutdown 帧返回 `{ok:true,stopped:true}` 后结束循环；CLI 入口对 market host-serve 跳过 envelope 渲染，stdout 仅帧。market-host-serve 单元 4 项/39 断言通过。
- [x] 1.2 实现只读投影路由；owner=radar；scope=host-serve 读帧映射 marketCommand；依赖=1.1；验收=capabilities/reader/brief(latest+ref)/signal revision/evidence/coverage/watches/watch changes/catchup 游标/reading/compare/reviews index+ref/question context，payload 与 CLI --json data 同源，禁区全出口、ref 安全校验、not_found 不回退；验证=bun test test/unit/market-host-serve.test.ts。证据：2026-09-19 readResource 将 radar://market/* URI 路由到 marketCommand（payload=CommandResult.data），capabilities/views 声明 DSH required view（market_brief/market_reader）；路径段安全字符集校验（越权路径 resource_not_found）；signal 读取绑定 revision（缺失 signal_not_found 不回退）；catchup 游标/limit 校验（cursor_invalid/input_invalid）；question 1–2000 字符；reviews index 为最近 30 条摘要的本地有界投影。单元测试覆盖 capabilities/reader/brief/coverage/watches/reviews/catchup 分页/signal 缺失/越权 URI/未知资源；禁区出口由 policy 测试覆盖（signal 读取与 question context 均返回 content_blocked 且不复述内容）。
- [x] 1.3 实现 typed dispatch 与对账；owner=radar；scope=dispatch/lookup-receipt 帧；依赖=1.1；验收=仅 dsh.radar.intent.v1 proposal、createAssignment 映射、幂等重放原回执、idempotency_conflict、do_not_shoot rejected、lookup reconciled/null、其他 kind 具名拒绝零写入；验证=bun test test/unit/market-host-serve.test.ts。证据：2026-09-19 dispatchProposal 解析 dsh.radar.intent.v1（kind=proposal、0–1 个安全 opportunity ref、安全 idempotencyKey、confirmed 布尔、UNSAFE_BLOB 拒绝），映射 createAssignment：ready→submitted 回执含 assignmentRef，do_not_shoot→rejected 回执 reason 明示 do_not_shoot；同键同参重放返回原回执，同键异参 idempotency_conflict，无 profile profile_required，非 proposal kind intent_unsupported、schema 不符/不安全 ref intent_invalid（零写入）；lookupAssignmentReceipt 按原键返回 reconciled 回执或 null，不触发二次 dispatch。单元 2 项覆盖全部路径。
- [x] 1.4 集成回放与全量门；owner=radar；scope=test/integration/market-host-seam.test.ts；依赖=1.1–1.3；验收=真实子进程 stdio 帧回放（init→简报→缺口→catchup 翻页→dispatch 幂等→policy 拒绝路径）经 evidence runner 留 run 目录；typecheck、bun test 全绿；验证=bun run typecheck && bun test && bun run test:integration。证据：2026-09-19 集成测试以隔离 RADAR_HOME 子进程链种库（init/import-catalog×2/analyze/brief build/profile create/run fixture），再拉起真实 `market host-serve` 子进程回放 26 帧：capabilities/reader→briefs/latest（radar.market_brief.v1）→coverage（radar.market_source_gaps.v1）→catchup limit=1 逐页翻至 next_cursor=null（≥2 个不同 signal）→signal revision 详情→dispatch submitted（含 assignmentRef）→同键重放回执逐字节一致→lookup-receipt reconciled→畸形行 frame_invalid 后续帧正常→config set 封锁 topic 后同一 signal 读取返回 content_blocked→shutdown 后子进程退出码 0。bun run typecheck 退出 0；bun test 全量 382 项（381 pass/1 skip，3140 断言）；bun run test:integration 138 项（137 pass/1 skip，1299 断言）通过，证据 temp/integration-test-runs/2026-09-19T14-32-18-608Z-y7s7gd/。
- [x] 1.5 seam 合同文档与软件收口；owner=radar；scope=docs/interfaces/market-host-seam.md；依赖=1.4；验收=传输决策、帧 schema、URI 映射、dispatch/回执、错误码与禁止事项成文供 DSH 对接；strict OpenSpec 通过后归档；验证=openspec validate radar-market-host-seam-v1 --strict --no-interactive。证据：2026-09-19 docs/interfaces/market-host-seam.md 成文（启动/生命周期、radar.market.host.frame.v1 帧、URI→CLI 映射表、dispatch/回执语义、错误码清单、边界与禁止）；openspec validate radar-market-host-seam-v1 --strict 通过，openspec validate --all --strict 23/23 通过；软件门全绿后归档本 change。

## 2. 验证依据

软件验证证据：bun run typecheck 退出 0；bun test 全量 382 项（381 pass/1 skip，3140 断言）；bun run test:integration 138 项（137 pass/1 skip，1299 断言），run 目录 temp/integration-test-runs/2026-09-19T14-32-18-608Z-y7s7gd/。新增 unit market-host-serve 4 项/39 断言、integration market-host-seam 1 项/30 断言。真实 DSH 会话接入（adapter 子进程拉起、浏览器 Pane 真实消费）属外部门，不由本 change 推断完成。
